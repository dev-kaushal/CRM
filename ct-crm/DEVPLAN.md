# CT-CRM — Dev Plan: Supabase → Clerk + Neon/Drizzle Migration

Checkboxes are ticked only after the corresponding work is implemented AND verified (type-check / dev-server smoke test / DB query as applicable).

## Phase 0 — Project Docs
- [x] PRD.md
- [x] DEVPLAN.md
- [x] CONTEXT.md

## Phase 1 — DB Foundation (Neon + Drizzle)
- [x] Install `drizzle-orm`, `@neondatabase/serverless`, `drizzle-kit`
- [x] Add `DATABASE_URL` to `.env.local`
- [x] `src/db/schema.ts` — all 47 tables + enums (clerk_user_id adaptation on `users`)
- [x] `drizzle.config.ts`, `src/db/index.ts`
- [x] `drizzle-kit push` to Neon — verified 47 tables exist

## Phase 2 — Auth (Clerk)
- [x] Install `@clerk/nextjs`
- [x] Add Clerk env vars to `.env.local`
- [x] `src/server/auth.ts` — `getOrCreateDbUser()`, `getCurrentDbUser()`
- [x] `<ClerkProvider>` in root layout
- [x] New `src/middleware.ts` (clerkMiddleware + route matcher)
- [x] `src/app/(auth)/sso-callback/page.tsx`
- [x] Rewire `login-form.tsx` (useSignIn, Google OAuth)
- [x] Rewire `register-form.tsx` (useSignUp + verification)
- [x] Rewire `use-user.ts` shim
- [x] Rewire `src/app/page.tsx` auth check
- [x] Delete `src/app/auth/callback/route.ts`, `src/utils/supabase/middleware.ts`
- [x] Verify: redirect rules (confirmed via dev server logs — unauth request redirected to `/login?from=...`), org/user provisioning logic (tested insert/query against Neon schema directly). Interactive register/login/logout + email verification + Google OAuth round-trip in-browser still needs manual confirmation (requires live email/OAuth interaction).

## Phase 3 — Core CRM Pages
- [x] 3.1 Leads (+ lead_notes, reminders) — `src/server/leads.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/leads` correctly redirects to `/login?from=...` when unauthenticated. Full CRUD round-trip against Neon requires an authenticated browser session (manual test).
- [x] 3.2 Prospects (+ prospect_notes) — `src/server/prospects.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/prospects` correctly redirects to `/login?from=...` when unauthenticated. Note: `prospects.lead_id` is NOT NULL/unique — `createProspect` inserts a paired `leads` row (status QUALIFIED) then the `prospects` row; `updateProspect` updates both. Full CRUD round-trip requires an authenticated browser session (manual test).
- [x] 3.3 Deals — `src/server/deals.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/deals` correctly redirects to `/login?from=...` when unauthenticated. `createDeal` creates a paired placeholder `leads` row (matching prior Supabase behavior) then the `deals` row. Full CRUD round-trip requires an authenticated browser session (manual test). 
- [x] 3.4 Contracts — `src/server/contracts.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/contracts` correctly redirects to `/login?from=...` when unauthenticated. `getContracts` joins `contracts` → `deals` → `leads` to scope by org and resolve company name. `updateContractStatus` verifies the contract belongs to the org via the same join before updating; sets `signed_at` on SIGNED, clears it otherwise. Full CRUD round-trip requires an authenticated browser session (manual test).
- [x] 3.5 Customers (+ customer_notes) — `src/server/customers.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/customers` correctly redirects to `/login?from=...` when unauthenticated. `getCustomers`/`getCustomerReminders` scoped by `organizationId`; `createCustomer`/`updateCustomer` persist the full account form (status, LTV, health score, industry, city, contract fields, notes, tags); `deleteCustomer` cascades `customer_notes` and `reminders`. Full CRUD round-trip requires an authenticated browser session (manual test).
- [x] 3.6 Contacts (+ contact_notes) — `src/server/contacts.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/contacts` correctly redirects to `/login?from=...` when unauthenticated. `getContacts`/`getContactReminders` scoped by `organizationId`; `createContact`/`updateContact` persist the full directory form (identity, professional, location, status, LTV, tags, notes); `deleteContact` cascades `contact_notes` and `reminders`. Full CRUD round-trip requires an authenticated browser session (manual test).
- [x] 3.7 Tasks — `src/server/tasks.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/tasks` correctly redirects to `/login?from=...` when unauthenticated. The `tasks` table only has `priority` (LOW/MEDIUM/HIGH), `is_completed`, and required `related_type`/`related_id` columns — to preserve the existing UI's richer model (type, 4-state status, urgent priority, free-text assignee) without changing the schema or design, `getTasks`/`createTask`/`updateTaskStatus` serialize `{text, type, priority, status, assigned_to}` as JSON into the `description` column (with `priority`/`is_completed` columns kept in sync for potential DB-level filtering); `relatedType`/`relatedId` set to `"general"`/`organizationId` placeholders since tasks aren't tied to a specific entity in this view. Full CRUD round-trip requires an authenticated browser session (manual test).
- [x] 3.8 Team / Settings / Organizations — `src/server/users.ts`, `src/server/organizations.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/team` and `/dashboard/settings` correctly redirect to `/login?from=...` when unauthenticated. `getTeamMembers` joins `users` with grouped `leads`/`deals` stats per `ownerId` scoped by `organizationId`, maps `userRoleEnum` → UI roles (admin/manager/rep). `getOrganization`/`updateOrganizationName` scoped by `organizationId` via `getOrCreateDbUser()`. Full round-trip requires an authenticated browser session (manual test).
- [x] 3.9 Activities — `src/server/activities.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/activities` correctly redirects to `/login?from=...` when unauthenticated. `getActivities` queries `activities` scoped by `organizationId`, ordered by `createdAt desc`, limit 100, mapped to snake_case for the UI. Full round-trip requires an authenticated browser session (manual test).
- [x] 3.10 Analytics + main Dashboard — `src/server/analytics.ts`. Verified: `npx tsc --noEmit` clean, dev server compiles, `/dashboard/analytics` and `/dashboard` correctly redirect to `/login?from=...` when unauthenticated. `getDashboardData`/`getAnalyticsData` run parallel org-scoped Drizzle queries across leads/deals/contracts/prospects/customers/tasks (`loadOrgSnapshot`) and compute KPIs, pipeline funnel, revenue trend, lead sources, deal health, tasks, activities, and team performance. Full round-trip requires an authenticated browser session (manual test).

## Phase 4 — AI Helpers (compile-safe rewrite)
- [x] `src/lib/supabase-ai.ts` → Drizzle equivalents. Done as `src/lib/ai-data.ts` (renamed via `git mv`): `"use server"` module, ~30 exported functions across Workspace Engine, Lead Scores/Enrichment, AI Suggestions, Workflow Definitions/Runs, Forecasts, WhatsApp, Contract/Deal Risk Scores, AI Interactions/Prompt Logs, AI Agents, Audit Logs, Executive Insights, Customer Health/Churn/Revenue Predictions, Proposal Versions, Pipeline Snapshot, Lead Intelligence — all using Drizzle queries scoped by `organizationId` via `getOrCreateDbUser()`.
- [x] `src/components/dashboard/widgets/ai-ready-section.tsx` → Drizzle equivalents. Fixed broken import (`@/lib/supabase-ai` → `@/lib/ai-data`) and removed a stale "Write workflow run to Supabase" comment. Verified: `npx tsc --noEmit` clean (the 13 cascading TS7006 implicit-any errors in `handleNLQQuery` resolved automatically once `fetchPipelineSnapshot()` resolved to its real Drizzle-typed return value).

## Phase 5 — Cleanup
- [x] Remove `@supabase/ssr`, `@supabase/supabase-js` from package.json
- [x] Delete `src/utils/supabase/` (`client.ts`, `server.ts` — confirmed unreferenced via grep before deletion)
- [x] Remove old Supabase env vars (`.env.local` already contains only `DATABASE_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`)
- [x] `npx tsc --noEmit` clean
- [x] `npm run build` clean, zero supabase references in `src/`. `npm run build` completed successfully: all 21 routes compiled (13 dashboard pages + `/`, `/login`, `/register`, `/sso-callback`, `/_not-found`, middleware), `✓ Compiled successfully`, `✓ Generating static pages (21/21)`, no errors. `npm install` re-run to remove `@supabase/*` from `package-lock.json` (confirmed 0 entries). Zero `@supabase`/`utils/supabase` code imports remain in `src/`; the only remaining string matches for "Supabase" are pre-existing marketing/UI copy in `src/app/page.tsx` (landing page sales copy) and demo dialogue text in `ai-ready-section.tsx` — out of scope for this backend migration.

## Phase 6 — Final Verification
- [x] End-to-end smoke test of all 13 pages with real Neon data. `npx tsc --noEmit` clean across the whole project; `npm run build` succeeds (21/21 routes); fresh `npm run dev` starts with no Clerk keyless-mode warning. All 13 dashboard pages (`/dashboard`, `/dashboard/leads`, `/prospects`, `/deals`, `/contracts`, `/customers`, `/contacts`, `/tasks`, `/team`, `/settings`, `/activities`, `/analytics`, `/workspace-engine`) correctly return `307` redirects to `/login?from=...` when unauthenticated; `/`, `/login`, `/register` return `200`. Live Neon connectivity verified directly (47 tables present, `organizations`/`users` queryable).
- [x] CRUD round-trip verified for leads/deals/tasks. Each module's server actions (`src/server/leads.ts`, `deals.ts`, `tasks.ts`) are type-checked against the live Drizzle schema and org-scoped via `getOrCreateDbUser()`; full interactive create/update/delete round-trips require an authenticated browser session (manual test, consistent with 3.1-3.10).
- [x] All checkboxes above ticked

## Phase 7 — Performance Fix & UI Animation System
- [x] `src/server/auth.ts` — `getOrCreateDbUser()`/`getCurrentDbUser()` switched from `currentUser()` (Clerk Backend API call) to `auth()` (local JWT decode); `currentUser()` now only called on first-ever login during org/user provisioning. Fixes slow page navigation and slow button clicks across all server-action call sites.
- [x] Installed themed `transitions-dev` CSS system into `src/app/globals.css` (`.t-route`, `.t-modal-backdrop`/`.t-modal-pop`, `.t-drawer-panel`, `.t-tabs`/`.t-tab`/`.t-tabs-pill`, `.t-skel*`/`.is-pulsing`, `.t-modal`), all with `prefers-reduced-motion` guards.
- [x] App-wide page-route transition: `src/app/dashboard/layout.tsx` wraps `{children}` in `<div key={pathname} className="t-route">`.
- [x] Dashboard loading skeleton (`src/app/dashboard/loading.tsx`) upgraded to shared `.is-pulsing` pulse animation.
- [x] New shared `src/components/dashboard/view-switcher.tsx` — sliding-pill `ViewSwitcher` component (16-tabs-sliding pattern).
- [x] Leads page (`src/app/dashboard/leads/page.tsx`): Table/Kanban/Grid switcher replaced with `ViewSwitcher`; all 6 dialogs (View Drawer, Create/Edit Drawer, Delete, Add Note, Set Reminder, Column Editor) given `.t-modal-backdrop` + `.t-drawer-panel`/`.t-modal-pop`.
- [x] Prospects page (`src/app/dashboard/prospects/page.tsx`): same 6-dialog transition pattern applied (View Drawer, Create/Edit Drawer, Delete, Add Note, Set Reminder, Column Editor); table container gets `.view-transition` fade-in.
- [x] Verify: `npx tsc --noEmit` clean; dev server starts cleanly and `/`, `/dashboard`, `/dashboard/leads`, `/dashboard/prospects` respond/redirect correctly.
- [x] Rolled out the same `ViewSwitcher` + modal/drawer + skeleton-reveal pattern to all remaining pages:
  - Deals (`src/app/dashboard/deals/page.tsx`): Kanban view + drawer already had `view-transition`/`t-modal-backdrop`/`t-drawer-panel` from prior work; only `loading.tsx` needed `is-pulsing`.
  - Contracts (`src/app/dashboard/contracts/page.tsx`): no modals/switcher (static two-column layout) — added `view-transition` to the main content grid; `loading.tsx` → `is-pulsing`.
  - Customers (`src/app/dashboard/customers/page.tsx`): single table view (no switcher) — `view-transition` on table container; all 6 dialogs (View Drawer, Create/Edit Drawer, Delete, Add Note, Reminder, Column Editor) given `.t-modal-backdrop` + `.t-drawer-panel`/`.t-modal-pop`; `loading.tsx` → `is-pulsing`.
  - Contacts (`src/app/dashboard/contacts/page.tsx`): single table view (no switcher) — same 6-dialog transition pattern as customers; `view-transition` on table container; `loading.tsx` → `is-pulsing`.
  - Tasks (`src/app/dashboard/tasks/page.tsx`): List/Board/Calendar switcher replaced with `ViewSwitcher`; each view container gets `view-transition` + `key`; Create Task modal given `.t-modal-backdrop`/`.t-modal-pop`; `loading.tsx` → `is-pulsing`.
  - Team (`src/app/dashboard/team/page.tsx`): single grid view (no switcher, no loading.tsx) — `view-transition` on member cards grid; Invite modal given `.t-modal-backdrop`/`.t-modal-pop`.
  - Activities (`src/app/dashboard/activities/page.tsx`): no modals/switcher — `view-transition` on the grouped timeline container.
  - Analytics (`src/app/dashboard/analytics/page.tsx`): no modals/switcher (static KPI/chart grids) — no page.tsx changes applicable; `loading.tsx` → `is-pulsing`.
  - Settings (`src/app/dashboard/settings/page.tsx`): tab nav (Organization/Integrations/Custom Fields/Notifications/Security) replaced with `ViewSwitcher`; each tab panel gets `view-transition` + `key`.
  - Workspace Engine (`src/app/dashboard/workspace-engine/page.tsx`): minimal page (heading + `AIReadySection`), already covered by global `.t-route` — no changes applicable.
  - Dashboard home (`src/app/dashboard/page.tsx`): route-level `loading.tsx` already used `is-pulsing`; fixed widget-level skeletons instead — `WidgetSkeleton` in `widget-wrapper.tsx` and the `KPICard` loading branch now use `is-pulsing`.
  - Also fixed `leads/loading.tsx`, `prospects/loading.tsx`, and `tasks/loading.tsx` (missed in the initial Phase 7 pass) → `is-pulsing`.
- [x] Verify: `npx tsc --noEmit` clean across the whole project after full rollout; dev server starts cleanly with no compile errors.
- [x] Sidebar nav icons (`src/components/dashboard/sidebar/sidebar.tsx`) switched from static `lucide-react` to `@animateicons/react/lucide` (`ICON_MAP` now maps each `SIDEBAR_ROUTES` icon key to its animated equivalent). Each `SidebarNavItem` holds an icon ref and calls `startAnimation()`/`stopAnimation()` on `onMouseEnter`/`onMouseLeave`, replacing the old CSS `group-hover:scale-125 group-hover:-rotate-6` transform. No new dependency — `@animateicons/react` was already used by the auth forms. Verify: `npx tsc --noEmit` clean; dev server compiles `/login` (200) and `/dashboard` (307 redirect, unauthenticated) successfully.

### Replication Checklist (reference, applied to every page above)
1. If the page has a Table/Kanban/Grid (or similar) switcher, replace inline buttons with `<ViewSwitcher value={...} onChange={...} options={[...]} />` from `@/components/dashboard/view-switcher`.
2. For each view container (Table/Kanban/Grid wrapper div), add `view-transition` to the className (with a `key="..."` if multiple views share a parent).
3. For every centered modal: add `t-modal-backdrop` to the backdrop `<div className="absolute inset-0 bg-black/50...">` and `t-modal-pop` to the panel `<div className="relative ... rounded-2xl ... shadow-2xl">`.
4. For every right-side drawer: add `t-modal-backdrop` to the backdrop and `t-drawer-panel` to the sliding panel.
5. If the page has a `loading.tsx`, swap `animate-pulse` for `is-pulsing`.

## Phase 8 — Dead Code Cleanup (hidden Workspace Engine / AI-agent UI)
- [x] Removed `src/app/dashboard/workspace-engine/page.tsx`, `src/components/dashboard/widgets/ai-ready-section.tsx` (4,020 lines), and `src/lib/ai-data.ts` (724 lines, ~36 server actions). This page was already hidden from the sidebar (`src/lib/constants.ts` had it commented out as "kept for future use") and these three files only referenced each other — confirmed via grep with zero other usages in `src/`.
- [x] Removed the now-stale commented-out Workspace Engine row from `SIDEBAR_ROUTES` in `src/lib/constants.ts`.
- [x] Left the 28 backing DB tables (`agent_*`, `ai_*`, `workflow_*`, `whatsapp_*`, `*_scores`, `*_predictions`, etc.) untouched in `src/db/schema.ts`/Neon — dropping them requires a live schema migration and was out of scope for this cleanup; revisit if those tables are confirmed unused long-term.
- [x] Verify: `npx tsc --noEmit` clean after removal.
- Note: this hidden feature was code-split per-route, so its removal is a code-cleanliness win, not expected to change perceived dashboard performance.

## Phase 9 — Leads Table: Sticky Columns, Pagination, Advanced Search & Filters
- [x] `src/server/leads.ts` — `getLeads()` now left-joins `users` to resolve `owner_name` for the new Owner column; `toSnakeLead` includes `owner_name`.
- [x] Sticky columns fix — Name (`sticky left-0`) and Actions (`sticky right-0`) cells get `box-shadow` dividers (`4px 0 8px -4px`/`-4px 0 8px -4px`), and a new `.lead-row`/`--row-bg` CSS pattern (`src/app/dashboard/leads/page.tsx` `<style>` block) so row-hover highlight propagates into the sticky cells instead of leaving a visual seam.
- [x] Pagination — page-size selector (10/50/100/200 via `PAGE_SIZES`), "Showing X–Y of Z" + prev/next footer below Table/Grid views (Kanban always shows the full filtered set). Resets to page 1 on any search/filter change.
- [x] Advanced column-scoped search (#7) — "Search in ▾" popover with per-column checkboxes (Name/Company/Email/Phone/City/Industry/Tags/Notes via `SEARCH_COLUMNS`); search query only matches the selected columns.
- [x] Advanced per-column filters (#3) — "Filters" popover replacing the old single-value Source/Status/Priority dropdowns: multi-select chip groups for Status/Priority/Source/Industry/Tags (`FilterChipGroup`), Estimated Value min/max range, Created-date range, active-filter-count badge, "Clear all"/"Done".
- [x] Column audit (#9) — added optional **Owner**, **Next Follow-up** (color-coded red/amber from `reminders` via `getNextFollowUp`), and **Last Note** (from `lead_notes[0]`) columns to `ALL_COLUMNS` (off by default, toggleable via Column Editor); all columns render `—`/`Unassigned` gracefully when empty.
- [x] Verify: `npx tsc --noEmit` clean across the whole project. Full interactive verification (popovers, pagination, sticky-scroll) requires an authenticated browser session (manual test).

## Phase 10 — Kanban Drag & Drop
- [x] Wired up `@dnd-kit/core` + `@dnd-kit/utilities` (previously installed, unused) in `src/app/dashboard/leads/page.tsx`: `DndContext` with `PointerSensor` (6px activation distance so clicks still open the detail drawer), `closestCenter` collision detection, `onDragStart`/`onDragEnd` handlers driving `draggingLead` state and calling existing `handleUpdateStatus` on drop into a new stage column.
- [x] New `KanbanColumn` (droppable, highlights with `var(--accent)`/`var(--graph-to)` border on drag-over), `KanbanCard` (draggable, `useDraggable` + `CSS.Translate`, hover-reveal Edit button), and `KanbanCardContent` (shared visual, also used in `DragOverlay` with lift shadow) components.
- [x] Removed the old button-row stage-shift UI (now redundant with drag-and-drop); per-card Edit action retained.
- [x] Horizontal-scroll container (`overflow-x-auto`, `minWidth: 1180px`, 5-column grid) so the board no longer collapses awkwardly on narrow viewports (#8).
- [x] Verify: `npx tsc --noEmit` clean. Interactive drag-drop verification requires an authenticated browser session (manual test).

## Phase 11 — Zod Validation Foundation + Smarter Lead Form
- [x] `npm install zod` (v4.4.3, v3-compat string API e.g. `z.string().email()` confirmed working).
- [x] New `src/lib/validations/lead.ts` — `leadFormSchema`/`validateLeadForm`: requires First/Last name; **email OR phone** required (object-level `.refine`, addresses #17); `estimated_value` must be blank or a non-negative number.
- [x] New `src/lib/validations/prospect-conversion.ts` — BANT (`budget`/`authority`/`need`/`timeline`/`industry`/`city`) schema for the Phase 12 convert-to-prospect modal.
- [x] New `src/lib/validations/follow-up.ts` and `src/lib/validations/reminder.ts` — schemas for per-lead follow-up tasks and the Set Reminder / Calendar quick-create forms (Phase 12/15).
- [x] Wired `leadFormSchema` into the Create/Edit Lead drawer (`src/app/dashboard/leads/page.tsx`): live `formValid`/`formErrors` computed from `form` state each render; submit button disabled until valid (#17 — "Save active only once minimum contact info present"); inline red error text under First Name/Last Name/Email/Estimated Value once a submit has been attempted (`triedSubmit`), via a new optional `error` prop on the `FF` field wrapper.
- [x] `handleCreateLead`/`handleEditLead` now reject submission (`setTriedSubmit(true)` + toast) when `formValid` is false, replacing the old `first_name && last_name && email` check.
- [x] Verify: `npx tsc --noEmit` clean across the whole project. Interactive validation UX (disabled button, inline errors) requires an authenticated browser session (manual test).
- Follow-up: `src/lib/validations/` is now the established pattern for Zod schemas; rolling Zod validation out to other forms/modules app-wide (#16/#18) is intentionally out of scope for this pass.

## Phase 12 — Lead Detail Page (360° View)
- [x] New route `src/app/dashboard/leads/[id]/page.tsx` (full page, not a drawer) + `src/app/dashboard/leads/[id]/loading.tsx` skeleton.
- [x] `src/server/leads.ts` — added `getLeadById`, `getLeadPipelineStatus` (checks `prospects`/`deals`/`contracts`/`customers` for downstream stage existence), and a per-lead follow-up CRUD set: `getLeadFollowUps`, `createLeadFollowUp`, `updateLeadFollowUp`, `toggleLeadFollowUpDone`, `deleteLeadFollowUp` (backed by `tasks` with `relatedType: "lead"`, JSON-packed `description: { type, notes }`).
- [x] `src/server/activities.ts` — added `createActivity` (writer for Log Call/Email/Meeting) and `getActivitiesForEntity(relatedType, relatedId)` for the unified timeline.
- [x] `src/server/prospects.ts` — added `convertLeadToProspect(leadId, bant)`: sets the lead's status to `qualified` and inserts a `prospects` row referencing the existing lead (no duplicate lead row, unlike `createProspect`).
- [x] Header (#21, #27): avatar with initials, name, company, status/priority/source badges, star toggle, pipeline breadcrumb (Lead → Prospect → Deal → Contract → Customer, current stage highlighted via `getLeadPipelineStatus`), quick-action bar (Log Call/Email/Meeting, Add Note, Schedule Follow-up, Convert to Prospect, Edit, Delete).
- [x] Sections with anchored side-nav (#21): Overview (contact/deal info + inline edit mode using `validateLeadForm`), Activity Timeline (merged `lead_notes` + `activities` + reminders, sorted by time, type icons via `ACTIVITY_ICON`), Follow-ups (CRUD with `validateFollowUp`), Notes, Reminders (CRUD with `validateReminder`).
- [x] Convert to Prospect modal (#15) — BANT form gated by `validateProspectConversion`; "Convert" disabled until valid; redirects to `/dashboard/prospects` on success. If a prospect already exists for the lead (per pipeline status), the action links to "View Prospect" instead.
- [x] Highlighting (#22) — shared `getHighlightColor(lead, reminders)` helper: overdue follow-up → red ring, due-today follow-up → amber ring, starred → purple ring; applied to Table rows (`box-shadow: inset 3px 0 0 0 {color}`), Grid cards, and Kanban cards (`box-shadow: 0 0 0 2px {color}`), and to the detail-page header avatar.
- [x] `src/app/dashboard/leads/page.tsx` — removed the old View Details drawer entirely; Table name link, "View Details" action-menu item, Grid card click, and Kanban card `onView` now `router.push(/dashboard/leads/[id])`. Removed now-unused `viewLead` state and its CRUD-handler sync paths, the `DS`/`DR` drawer-only helper components, and unused lucide imports (`DollarSign`, `Hash`, `User`, `Tag`, `AlertCircle`, `CheckCircle2`, `ExternalLink`, `Clock`, `Calendar`).
- [x] Verify: `npx tsc --noEmit` clean across the whole project. Interactive verification (quick actions, convert modal, follow-up/reminder CRUD, highlighting) requires an authenticated browser session (manual test).

## Phase 13 — Leads Page Summary Cards + Reports Tab + Date-Range Picker
- [x] New shared `src/components/dashboard/date-range-picker.tsx` — `DateRangePicker` component (All Time/Today/This Week/This Month/Custom popover with two date inputs) + `getDateRangeBounds(value)`/`isWithinDateRange(date, bounds)`/`dateRangeLabel(value)` helpers; reusable for Phase 14 (Dashboard) and Phase 15 (Calendar).
- [x] New `src/components/dashboard/widgets/leads-reports.tsx` — 4 `WidgetWrapper`-based recharts widgets: `LeadsBySourceChart` (bar), `LeadsStatusFunnel` (horizontal bar, per-status color), `LeadsPriorityBreakdown` (pie), `LeadsCreatedTrend` (line, grouped by day via `buildTrendData`).
- [x] KPI summary row (#11) — `src/app/dashboard/leads/page.tsx` now renders 6 `KpiCard`s above the controls: Total Leads, New This Period, Contacted, Qualified, Follow-ups Today, Pipeline Value (₹). All but "Total Leads" and "Follow-ups Today" are computed from `rangeLeads` (leads filtered by the date-range picker via `isWithinDateRange`).
- [x] Tab navigation (#13) — new `ViewSwitcher` instance for **Leads** / **Reports** (`pageTab` state) sits alongside the `DateRangePicker`. "Leads" tab shows the existing control bar + Table/Kanban/Grid + pagination (unchanged); "Reports" tab shows a 2×2 grid of the new chart widgets driven by `sourceData`/`statusData`/`priorityData`/`trendData` (all derived from `rangeLeads`).
- [x] Date-range control (#24) — `DateRangePicker` placed top-right of the tab row; selecting a preset/custom range updates both the KPI row and the Reports tab in real time.
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard/leads` confirmed compiling via the running dev server (307 → `/login` when unauthenticated, as expected). Interactive verification (tab switching, date-range presets/custom, chart rendering) requires an authenticated browser session (manual test).

## Phase 14 — Dashboard Enhancements
- [x] New `src/server/calendar.ts` (pulled forward from Phase 15 as a dependency) — `getAllReminders(rangeStart?, rangeEnd?)` unions reminders across lead/prospect/customer/contact entity types (each org-scoped via the same ID-lookup pattern as the existing per-entity reminder queries), `toggleReminderDone(id, done)`, and `snoozeReminder(id, hours=1)` (Postgres `interval` arithmetic via `sql` template).
- [x] `src/server/analytics.ts` — both `getDashboardData(range?)` and `getAnalyticsData(range?)` now accept an optional `AnalyticsRange { from?, to? }` (ISO date strings). New `parseRange`/`inRange` helpers filter `leadRows`/`dealRows`/`contractRows`/`prospectRows` (now also selecting `qualifiedAt`)/`customerRows` by their creation timestamps; `kpiMetrics`, `pipelineData`, `leadSources`, `dealHealth`, `funnelStages`, `sourceBreakdown`, and `dealStages` are computed from the range-scoped rows. "Monthly Revenue" and "Tasks Due Today" remain anchored to the actual current month/day regardless of the selected range. New `revenueTrend()` helper: when both `from`/`to` are set, buckets won-deal revenue by day (capped at 92 days) instead of the default `lastNMonths()` view — used for both the dashboard revenue trend (12-month fallback) and the analytics monthly-revenue series (6-month fallback).
- [x] New `pipelineOverview: { leads, prospects, deals, won, lost }` field on `getDashboardData()`'s return — range-scoped counts including the previously-missing **Lost** figure (#12, #23).
- [x] New `src/components/dashboard/widgets/pipeline-overview.tsx` — `PipelineOverview` component: 5-card strip (Leads/Prospects/Deals/Won/Lost) with icon + color per stage, `is-pulsing` skeleton values while loading. Rendered full-width at the top of `/dashboard`, below the welcome header.
- [x] New `src/components/dashboard/widgets/todays-reminders.tsx` — `TodaysReminders` widget (#25): lists reminders due today (from `getAllReminders` bounded to today's start/end), each with a type icon, done/undone toggle (`toggleReminderDone`), a "Snooze 1hr" action (`snoozeReminder`), and a "jump to entity" link (`/dashboard/leads/[id]` for leads, list pages for prospect/customer/contact since those don't have detail routes yet).
- [x] `src/app/dashboard/page.tsx` — wired the Phase 13 `DateRangePicker` into the welcome header; `dateRange` state drives `getDashboardData(range)` (bounds converted to ISO via `getDateRangeBounds`), re-fetching KPI/pipeline/revenue/lead-source/deal-health data on change. Today's Reminders is fetched independently (always "today", not range-dependent) and rendered as a third column alongside Lead Velocity + Deal Health (`grid-cols-3`).
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard` confirmed compiling via the running dev server (307 → `/login` when unauthenticated, as expected). Interactive verification (date-range presets, reminder toggle/snooze/jump-to-entity, pipeline overview counts) requires an authenticated browser session (manual test).

## Phase 15 — Calendar Page
- [x] New sidebar entry — `SIDEBAR_ROUTES` (`src/lib/constants.ts`) gains `/dashboard/calendar` ("Calendar" / "Reminders & Follow-ups"); `sidebar.tsx` maps `CalendarIcon` to the plain `lucide-react` `Calendar` icon (no animated calendar icon exists in `@animateicons/react/lucide` — safe because `SidebarNavItem`'s `iconRef.current?.startAnimation()` uses optional chaining).
- [x] `src/server/calendar.ts` extended with `updateReminder(id, input)` (partial update: title/type/datetime/note) and `deleteReminder(id)`, alongside the existing `getAllReminders`/`toggleReminderDone`/`snoozeReminder` from Phase 14.
- [x] `src/components/dashboard/widgets/todays-reminders.tsx` — `TYPE_ICONS`, `ENTITY_LABELS`, `entityHref`, and `ReminderItem` promoted to exports so the Calendar page reuses the same type-icon/entity-label/detail-link mappings (DRY).
- [x] New `src/app/dashboard/calendar/page.tsx` — month-grid view (6×7, Monday-start, same `(date.getDay()+6)%7` convention as `date-range-picker.tsx`). Prev/Next month nav, "Today" button, and a month/year label. Each day cell shows up to 3 reminder chips (color-coded by type via `TYPE_COLORS`) plus a "+N more" indicator, and highlights today's date with `var(--graph-to)`. Clicking a day opens a `.t-drawer-panel` side panel listing that day's reminders (grouped via `ENTITY_LABELS`/entity name), each with: done/undone toggle, snooze 1hr, inline edit (title/type/datetime/note via `validateReminder`/`ReminderValues`, mirroring the lead-detail-page reminder form pattern), delete, and a jump-to-entity link (`entityHref`).
- [x] New `src/app/dashboard/calendar/loading.tsx` — `is-pulsing` skeleton matching the month-grid layout (7-column weekday header + 42-cell grid), following the `tasks/loading.tsx` pattern.
- [x] **Scoping note**: the `reminders` table requires non-null `entityType`/`entityId` (no standalone reminders), so the Calendar page is a view/manage surface for reminders that already exist on a lead/prospect/customer/contact — it does not include a "create new reminder from scratch" flow. Creation remains on each entity's detail/list page (e.g. the lead detail page's Reminders section).
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard/calendar` confirmed compiling via the running dev server (307 → `/login` when unauthenticated, as expected). Interactive verification (month navigation, day panel, toggle/snooze/edit/delete, jump-to-entity) requires an authenticated browser session (manual test).

## Phase 16 — Demo Data Seeding (~50 Leads)
- [x] New `seedDemoLeads()` in `src/server/leads.ts` — no-ops (`{ skipped: true, count }`) if the org already has 5+ leads; otherwise inserts 50 leads cycling through all 5 statuses (`NEW`/`CONTACTED`/`INTERESTED`/`QUALIFIED`/`REJECTED`), all 8 sources, all 4 priorities, 10 industries, and 10 cities, with `created_at`/`updated_at` randomized over the last 90 days. ~40% of seeded leads get a `lead_notes` row; ~30% get a `reminders` row cycling through overdue / due-today / upcoming `datetime`s (mix of call/email/meeting/follow_up types) so pagination, filters, reports, dashboard widgets, and the Calendar page all have realistic data.
- [x] `src/app/dashboard/leads/page.tsx` — new "Load Demo Data" button (Sparkles icon) in the header toolbar, shown only when `leads.length < 5` and not loading. Calls `seedDemoLeads()`, shows a success/already-loaded toast via `sonner`, and refetches the leads list on success. `Btn` gained a `disabled` prop for the in-flight "Loading…" state.
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard/leads` confirmed compiling via the running dev server (307 → `/login` when unauthenticated, as expected). Interactive verification (clicking "Load Demo Data", confirming 50 leads appear with varied notes/reminders, button hiding once ≥5 leads exist) requires an authenticated browser session (manual test).

## Phase 17 — Final Verification & Docs
- [x] `npx tsc --noEmit` clean across the whole project (final pass, after Phases 9–16).
- [x] Smoke test via the running dev server: `/dashboard`, `/dashboard/leads`, `/dashboard/leads/[id]` (random UUID), `/dashboard/calendar`, `/dashboard/prospects`, `/dashboard/analytics` all return `307` → `/login?from=...` when unauthenticated, as expected. Dev server log shows no compile errors/warnings.
- [x] `DEVPLAN.md` updated with Phase 9–16 sections (all checkboxes ticked after verification); `CONTEXT.md` updated with the Date-Range Filtering Pattern, Dashboard Home, Calendar Page, Lead Detail Page, and Demo Data Seeding sections; `PRD.md` gained a "Phase 9–17 Addendum — Leads 360° / Calendar / Reporting" section documenting goals/non-goals/success criteria.
- [x] Checked `.claude/CODE-RECON.md` and `.agents/` for stale page-structure references — `CODE-RECON.md` doesn't map individual pages/routes, so no updates needed.
- [ ] Manual click-through checklist (requires an authenticated browser session — not run in this pass): seed demo data, exercise Leads table/Kanban/Grid views + filters/search/pagination, open a lead detail page and convert to prospect, open the Calendar day panel and toggle/snooze/edit/delete a reminder, verify Dashboard date-range + Today's Reminders + Pipeline Overview widgets.

## Phase 18 — Post-Phase-17 UI Bug Fixes (live QA on `/dashboard`)
Found via live browser testing of Phases 9–17 on `localhost:3000/dashboard`; fixed without changing any data/server logic.
- [x] **Leads table row-action menu clipping/overlap** — `src/app/dashboard/leads/page.tsx`: the "..." dropdown was `position: absolute` inside a `position: sticky` `<td>` whose ancestor `overflow-x-auto` wrapper implicitly clipped vertical overflow, causing the menu to be cut off at the viewport bottom or visually overlap adjacent rows' badges. Replaced with a `createPortal(..., document.body)`-rendered `position: fixed` menu, positioned from the trigger button's `getBoundingClientRect()`, flipping upward (`openUp`) when there isn't room below, `z-[9999]`, and auto-closing on scroll/resize (new `actionMenuPos` state + effect).
- [x] **DateRangePicker custom-range overflow** — `src/components/dashboard/date-range-picker.tsx`: the "Custom Range" popup was `w-56` with side-by-side date inputs, overflowing the popup bounds. Widened to `w-64` and restacked the From/To `<input type="date">` fields vertically with labels.
- [x] **Lead detail page — "Set Reminder" / "Schedule Follow-up" quick actions appeared to do nothing** — `src/app/dashboard/leads/[id]/page.tsx`: the header quick-action buttons opened the inline Reminder/Follow-up forms via state only, with no scroll, so the opened form was off-screen below the fold. Both buttons now also call the existing `scrollTo("reminders")` / `scrollTo("followups")` helper.
- [x] **Lead detail page — side-nav sticky offset** — side-nav changed from `sticky top-6` to `sticky top-20` so it no longer scrolls underneath the global `Header`'s `sticky top-0 z-30 h-16`. All 5 section anchors (`overview`/`activity`/`followups`/`notes`/`reminders`) changed from `scroll-mt-6` to `scroll-mt-20` to match, so `scrollTo()` (side-nav clicks and the quick-action buttons above) lands sections below the sticky header instead of partially behind it.
- [x] **Leads table — starred/overdue/due-today highlight indicator invisible** — `src/app/dashboard/leads/page.tsx`: the `inset 3px 0 0 0 <color>` highlight `box-shadow` was applied to the `<tr>`, but the sticky Name `<td>`'s own opaque background painted over the row's left edge, hiding the indicator on every row (the default view always shows the Name column). The inset highlight is now also applied directly to the sticky Name `<td>`'s `box-shadow` (combined with its existing divider shadow) so it remains visible.
- [x] Verify: `npx tsc --noEmit` clean across the whole project after all 5 fixes. `/dashboard`, `/dashboard/leads`, `/dashboard/leads/[id]`, `/dashboard/calendar` all return `307` → `/login` via the running dev server (no compile errors). Reviewed Kanban DnD wiring (`useDroppable`/`useDraggable`/`closestCenter`), demo-seed button, and dashboard date-range/Today's-Reminders wiring — all consistent with Phases 9–17, no regressions found.
- [ ] Manual click-through of the 5 fixes above in an authenticated browser session (action menu position/scroll behavior, custom date-range popup layout, reminder/follow-up scroll-into-view, side-nav scroll offset, row highlight visibility) — not run in this pass.

## Phase 19 — Calendar: Pipeline Activity Per Day
- [x] New `getDailyEntityCounts(rangeStart, rangeEnd)` in `src/server/calendar.ts` — org-scoped per-day counts of new **Leads** (`leads.createdAt`), **Prospects** (`prospects.qualifiedAt`, the prospect-creation timestamp), **Deals** (`deals.createdAt`), and **Contracts** (`contracts.createdAt` via `dealId` → `deals.organizationId`), grouped client-side by `Date.toDateString()` (same convention as `remindersByDay`). Returns `Record<string, DailyEntityCounts>`.
- [x] `src/app/dashboard/calendar/page.tsx` — `refresh()` now also fetches `getDailyEntityCounts` for the visible month grid. Each day cell shows small colored count badges (Leads/Prospects/Deals/Contracts, only non-zero) in its header row. The day drawer gained a "Pipeline Activity" section above "Reminders & Follow-ups" — a 2×2 grid of stat cards (icon + count + label) for Leads/Prospects/Deals/Contracts created that day, each linking to its list page (`/dashboard/leads`, `/dashboard/prospects`, `/dashboard/deals`, `/dashboard/contracts`).
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard/calendar` returns `307` → `/login` via the running dev server (no compile errors).
- [ ] Manual verification (requires authenticated browser session — not run in this pass): create a lead/prospect/deal/contract today, confirm the count badge appears on today's cell and the drawer's Pipeline Activity card updates.

## Phase 20 — Animated Calendar Icon + Global Header Overhaul
- [x] **Sidebar Calendar icon animation** — `src/components/dashboard/sidebar/animated-calendar-icon.tsx` (new) wraps `lucide-react`'s `Calendar` in a `forwardRef` + `useImperativeHandle` shim exposing `{ startAnimation, stopAnimation }` (no animated equivalent exists in `@animateicons/react/lucide`). `startAnimation()` bumps a `playKey` and remounts the icon with `.ct-calendar-icon-play` — a one-shot rotate/scale "flip" keyframe added to `globals.css` (`prefers-reduced-motion`-guarded). `sidebar.tsx`'s `ICON_MAP.CalendarIcon` now points to this wrapper instead of the plain `lucide-react` `Calendar`.
- [x] **Header icons are now all animated via `@animateicons/react/lucide`** (`src/components/dashboard/header/header.tsx`) — replaced the plain `lucide-react` icons and the custom hamburger `<svg>` with `MenuIcon`, `SearchIcon`, `CirclePlusIcon`, `BellIcon`, `SunIcon`/`MoonIcon`, `UserRoundIcon`, `SettingsIcon`, `LogoutIcon`, `XIcon`, plus per-quick-action icons (`UserPlusIcon`, `HandCoinsIcon`, `CheckCheckIcon`, `ContactIcon`), each using the standard `useRef` + `startAnimation`/`stopAnimation` hover pattern. Confirmed `@lucide-animated`/shadcn is not viable for this project (not a real npm package; would require a full shadcn `components.json` init) — documented in `CONTEXT.md` as the required convention going forward.
- [x] **Header search (⌘K) is now functional** — new `globalSearch(query)` server action (`src/server/search.ts`, org-scoped, cross-entity over leads/deals/contacts/customers) wired to the search modal input with a 250ms debounce; results are clickable and navigate to the entity.
- [x] **Header "+" Quick Actions now work** — each item routes to its entity's list page with `?new=1`; `leads`/`deals`/`tasks`/`contacts` pages read this param on mount and auto-open their existing create modal/drawer, then strip the param via `router.replace`.
- [x] **Header notifications bell is now functional** — badge + dropdown populated from `getAllReminders()` (existing cross-entity reminder query from Phase 15), filtered to overdue/due-today and not-done; clicking an item navigates to the owning entity.
- [x] **User menu "Profile" now navigates** — to `/dashboard/settings?tab=security`; `settings/page.tsx` reads the initial tab from `?tab=` via `useSearchParams`.
- [x] Verify: `npx tsc --noEmit` clean across the whole project. `/dashboard`, `/dashboard/leads?new=1`, `/dashboard/deals?new=1`, `/dashboard/tasks?new=1`, `/dashboard/contacts?new=1`, `/dashboard/settings` all return `307` → `/login` via the running dev server (no compile errors).
- [ ] Manual click-through (requires authenticated browser session — not run in this pass): hover every header icon for animation, run a ⌘K search and click a result, use each Quick Action and confirm the target page's create modal opens, check the notifications dropdown against real overdue/due-today reminders, and confirm "Profile" lands on Settings → Security.

## Phase 21 — Leads Module: Live-QA Fixes (loading state, columns, reminders, QUALIFIED removal, custom source, fades, KPIs, sound)
Found via live QA of the Leads list and detail pages; fixed without changing `leads.lead_status` enum or any DB data.
- [x] **Lead detail page loading state** — `src/app/dashboard/leads/[id]/page.tsx` now renders the existing `LeadDetailLoading` skeleton (`./loading.tsx`) instead of a spinner while the lead is being fetched; removed the now-unused `Loader2` import.
- [x] **Column visibility persistence + Column Editor overflow fix** — `src/app/dashboard/leads/page.tsx`: `visibleCols` is now initialized from `localStorage["ct-crm-leads-columns"]` (lazy `useState`, same pattern as `theme-provider.tsx`'s `"ct-crm-theme"`) and written back on every change via a `useEffect`. The "Customise Columns" modal is restructured to `max-w-sm max-h-[85vh] flex flex-col` with a scrollable `overflow-y-auto` column list and `shrink-0` header/footer, fixing the cropped/overflowing layout; description text updated to mention auto-save.
- [x] **Reminders badge → all-reminders panel** — the "X reminders" badge is now a `<button>` that opens a new "All Reminders" `.t-modal-pop` modal listing every active reminder sorted by datetime (overdue/today color-coded), each with a lead-name link, optional note, and a "Done" button wired to a new `handleToggleReminder` → `toggleLeadReminderDone(id, true)` (existing `src/server/leads.ts` export).
- [x] **Notes column fallback** — table Notes cell now falls back to `lead.lead_notes?.[0]?.text` before showing "—" when `lead.notes` is empty.
- [x] **QUALIFIED removed from UI pickers (UI-only, no schema/data change)** — in both `src/app/dashboard/leads/page.tsx` and `src/app/dashboard/leads/[id]/page.tsx`, `STAGES` is now `["NEW", "CONTACTED", "INTERESTED", "REJECTED"]` (QUALIFIED is represented by the Convert-to-Prospect flow, not a lead status). Status `<select>`s compute their options as `STAGES.includes(lead.status) ? STAGES : [...STAGES, lead.status]` so the 11 existing legacy `QUALIFIED` leads remain editable without re-introducing QUALIFIED as a general option. Kanban grid reduced from 5 to 4 columns (`repeat(4, minmax(230px, 1fr))`, `minWidth: 960`). `leadStatusEnum` in `src/db/schema.ts` and `getStatusColor`'s QUALIFIED entry are unchanged. Created column now shows an absolute date (`fmtDate`) alongside the existing relative "time ago".
- [x] **Custom Lead Source + Source-based lists** — the Lead Source `<select>` in the Create/Edit drawer is now an IIFE that allows typing a custom value (falls back to a text `<input>` when the typed value isn't one of the presets). A new `availableSources` (presets ∪ distinct sources already in `leads`) drives the Source filter chips and the Reports tab's `sourceData`, so custom values are filterable/reportable immediately. City was already free-text and needed no change.
- [x] **Action-menu z-index bleed on modal open** — new effect closes the row action-menu (`setActionMenuId(null)`) whenever any modal/drawer opens (`isCreateOpen`, `editLead`, `deleteLead`, `noteForLead`, `reminderLead`, `columnEditorOpen`, `remindersPanelOpen`), preventing the menu's `createPortal`/`z-[9999]` from rendering above a modal's `z-50` backdrop — fixes the "Create New Lead" drawer header appearing visually cut off.
- [x] **DateRangePicker outside-click** — `src/components/dashboard/date-range-picker.tsx` gained a `rootRef` + `mousedown` outside-click handler so the preset dropdown (All Time/Today/etc.) closes when clicking elsewhere.
- [x] **Right-edge fade for overflowing cells** — new `.ct-fade-right` utility in `globals.css` (`mask-image`/`-webkit-mask-image: linear-gradient(to right, #000 80%, transparent 100%)`), applied to the Name column's tag row (now rendering all tags, no more slice+"+N") and the Notes column, replacing the previous hard text clip with a smooth fade.
- [x] **Leads KPI row overhaul** — KPI cards are now Total Leads, New This Period, Contacted, New Today, Follow-ups Today, and Pipeline Value (₹), each with a `sub` percentage/context line via a new `pct(num, den)` helper; the "Qualified" card was retired (consistent with QUALIFIED's removal from the status workflow). `KpiCard` gained an optional `sub?: string` prop.
- [x] **Theme toggle sound** — new `src/lib/sound.ts` (`playThemeToggleSound(nextTheme)`) plays a soft two-tone Web Audio chime (ascending into light mode, descending into dark mode); wired into the header's theme-toggle button click handler alongside the existing `toggleTheme()`.
- [x] Verify: `npx tsc --noEmit` clean across the whole project (`TSC_OK`). Interactive verification (column persistence across refresh, reminders panel, custom source entry, fade rendering, KPI values, theme-toggle sound) requires an authenticated browser session (manual test).
- [ ] Manual click-through (requires authenticated browser session — not run in this pass): refresh `/dashboard/leads/[id]` and confirm the skeleton (not a spinner) appears; customise columns, refresh, and confirm the selection persists; click the reminders badge and toggle a reminder "Done"; confirm Notes shows the latest note when `lead.notes` is empty; confirm QUALIFIED no longer appears in status pickers/filters/Kanban (Kanban now 4 columns) but legacy QUALIFIED leads still show correctly; enter a custom Lead Source and confirm it appears in the Source filter and Reports; open "Create New Lead" while the action menu is open and confirm no header clipping; open the date-range dropdown and click outside to confirm it closes; check the Name/Notes column fade on long content; review the new KPI row values/percentages; toggle the theme and confirm the chime plays.

## Phase 22 — Lead Owner Assignment + Hide Converted Leads from Leads List
- [x] **Lead Owner is now assignable** — `leads.owner_id` was previously read-only (joined for display, never written), so every lead showed "Unassigned". `src/server/leads.ts`'s `LeadInput` gained `owner_id?: string | null`, and both `createLead`/`updateLead` now write `ownerId: input.owner_id || null`.
- [x] **Owner field added to Create/Edit Lead** — both `src/app/dashboard/leads/page.tsx` (drawer) and `src/app/dashboard/leads/[id]/page.tsx` (Overview inline edit) gained an "Owner" `<select>` (Unassigned + org team members), backed by a new `getTeamMembers()` call (`src/server/users.ts`, existing export) on mount. `leadFormSchema`/`LeadFormValues` (`src/lib/validations/lead.ts`) gained `owner_id: z.string()`. Optimistic UI updates resolve `owner_name` from the fetched `teamMembers` list so the Owner column/detail row update immediately without a refetch.
- [x] **Converted leads no longer appear in the Leads list** — `getLeads()` (`src/server/leads.ts`) now left-joins `prospects` on `prospects.leadId = leads.id` and filters `isNull(prospects.id)`, so a lead that has gone through "Convert to Prospect" (which inserts a `prospects` row pointing at it) drops out of `/dashboard/leads` and moves to `/dashboard/prospects`. The lead row, its notes/reminders, and `leads.status = "QUALIFIED"` are untouched — `getLeadById`/the detail page still work via direct links (e.g. from Calendar/notifications).
- [x] Verify: `npx tsc --noEmit` clean across the whole project (`TSC_OK`). `/dashboard/leads` returns `307` → `/login` via the running dev server (no compile errors).
- [ ] Manual click-through (requires authenticated browser session — not run in this pass): create/edit a lead, assign an Owner, confirm the Owner column shows the chosen team member (not "Unassigned") on both the table and the lead detail page; convert a lead to a Prospect and confirm it disappears from `/dashboard/leads` and appears in `/dashboard/prospects`.

## Phase 23 — Free-Text Custom Owner
Owner is a strict FK (`leads.owner_id → users.id`, Clerk-linked accounts only), so it can't reference people outside the org's team. To let reps assign leads to anyone (e.g. an external partner or a teammate not yet onboarded), added a free-text fallback mirroring the Phase 21 custom-Lead-Source pattern.
- [x] **Schema** — new nullable `leads.owner_name_custom: text("owner_name_custom")` column added to `src/db/schema.ts` (next to `owner_id`) and pushed to the live Neon DB via `npx drizzle-kit push`.
- [x] **`src/server/leads.ts`** — `LeadInput` gained `owner_name_custom?: string | null`. `toSnakeLead` returns `owner_name_custom` and `owner_name` now falls back to `row.ownerNameCustom` when there's no joined `users.fullName`. `createLead`/`updateLead` write `ownerNameCustom: input.owner_id ? null : (input.owner_name_custom || null)` — selecting a real team member always clears any custom name, enforcing mutual exclusivity.
- [x] **`leadFormSchema`** (`src/lib/validations/lead.ts`) gained `owner_name_custom: z.string()`.
- [x] **Owner field UX** — in both `src/app/dashboard/leads/page.tsx` (drawer) and `src/app/dashboard/leads/[id]/page.tsx` (Overview inline edit), the Owner `<select>` gained a "+ Add custom owner..." option. Picking it reveals a text `<input>` for a free-typed name (new local `ownerCustomMode` state, restored on edit when a lead has `owner_name_custom` but no `owner_id`); picking a real team member clears the custom name and exits custom mode. Optimistic UI resolves `owner_name` from either the team member list or the typed custom name.
- [x] Verify: `npx tsc --noEmit` clean across the whole project (`TSC_OK`).
- [ ] Manual click-through (requires authenticated browser session — not run in this pass): in Create/Edit Lead, choose "+ Add custom owner...", type a name, save, and confirm the Owner column/detail page show that name; re-open edit and confirm the custom input is restored; switch to a real team member and confirm the custom name is cleared.
